---
type: local
engine: toc
schema: %CURRENT_FDB_DIR%/%DEFAULT.EXPID%/fdb/NATIVE_grids/etc/fdb/schema
spaces:
- handler: Default
  roots:
  - path: %CURRENT_FDB_DIR%/%DEFAULT.EXPID%/fdb/NATIVE_grids
    wipe: true
    archive: true
    list: true
    retrieve: true

