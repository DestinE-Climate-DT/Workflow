CONFIG:
  AUTOSUBMIT_VERSION: "4.0.0b0"
  TOTALJOBS: 20
  MAXWAITINGJOBS: 20
DEFAULT:
  EXPID: "a01z"
  HPCARCH: "Lumi"
  #hint: use %PROJDIR% to point to the project folder (where the project is cloned)
  CUSTOM_CONFIG:
    PRE:
     - "%PROJDIR%/conf"
     - "%PROJDIR%/conf/simulation/%RUN.SIMULATION%.yml"
     - "%PROJDIR%/conf/parameter/%RUN.PARAMETER%.yml"
     - "%PROJDIR%/conf/model/%RUN.MODEL%/%RUN.MODEL%.yml"
     - "%PROJDIR%/conf/model/%RUN.MODEL%/%RUN.GRID%.yml"
     - "%PROJDIR%/conf/application/%RUN.APP%/%RUN.APP%.yml"
     - "%PROJDIR%/conf/application/%RUN.APP%/gsvrequest.yml"
     - "%PROJDIR%/conf/application/%RUN.APP%/oparequest.yml"
    POST:
     - "~/platforms.yml"

PROJECT:
  PROJECT_TYPE: "git"
  PROJECT_DESTINATION: "workflow"
GIT:
  PROJECT_ORIGIN: "https://earth.bsc.es/gitlab/digital-twins/de_340/workflow.git"
  PROJECT_BRANCH: "app-integrate-urban"
  PROJECT_COMMIT: ""
  PROJECT_SUBMODULES: ""
  FETCH_SINGLE_BRANCH: True

experiment:
  DATELIST: 20200120
  MEMBERS: fc0
  CHUNKSIZEUNIT: day
  CHUNKSIZE: 1
  NUMCHUNKS: 4
  CALENDAR: standard
