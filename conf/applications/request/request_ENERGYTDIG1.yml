ENERGYTDIG1:
  1:
    GSVREQUEST:
      # From <expid>/conf/main.yml:
      expver: "%REQUEST.EXPVER%"
      # From user input <expid>/conf/request.yml:
      activity: "%REQUEST.ACTIVITY%"
      resolution: "%REQUEST.RESOLUTION%"
      generation: "%REQUEST.GENERATION%"
      realization: "%REQUEST.REALIZATION%"
      experiment: "%REQUEST.EXPERIMENT%"
      model: "%REQUEST.MODEL%"
      # Hardcoded parameters:
      dataset: climate-dt
      class: d1
      type: fc
      stream: clte
      levtype: "hl"
      levelist: 100
      date: "%SPLIT_START_DATE%"
      time: "0000/to/2300/by/0100"
      param: "ws"
      grid: "%ENERGY_INDICATORS.GRID%"
      method: "%ENERGY_INDICATORS.METHOD%"
    OPAREQUEST:
      # Hardcoded parameters
      variable: "ws"
      stat: "percentile"
      percentile_list: [0.99]
      stat_freq: "monthly"
      output_freq: "monthly"
      time_step: 60 # in minutes
