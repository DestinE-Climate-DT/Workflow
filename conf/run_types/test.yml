MODEL:
  ROOT_PATH: "%CONFIGURATION.HPC_PROJECT_DIR%/models/%MODEL.NAME%"
  PATH: "%MODEL.ROOT_PATH%/%^MODEL.VERSION%/make/%CURRENT_HPCARCH_lowercase%-%RUN.ENVIRONMENT%"
  INPUTS: "%MODEL.ROOT_PATH%/%^MODEL.VERSION%/inidata"
REQUEST:
  EXPVER: "%DEFAULT.EXPID%"
  DATASET: climate-dt
  STREAM: clte
  CLASS: "d1"
  FDB_HOME: "%CONFIGURATION.FDB_DIR%/%REQUEST.EXPVER%/fdb"
  INFO_FILE_PATH: "%REQUEST.FDB_HOME%"
  INFO_FILE_NAME: "%REQUEST.INFO_FILE_PATH%/%DEFAULT.EXPID%.yaml"
APP_AUX_IN_DATA_DIR: "%CURRENT_TEST_APP_AUX_IN_DATA_DIR%"
HYDROLAND:
  IN_DATA_VERSION: "dev"
HYDROMET:
  IN_DATA_VERSION: "1.2"
WILDFIRES_WISE:
  IN_DATA_VERSION: "1.0"
ENERGY_OFFSHORE:
  IN_DATA_VERSION: "dev"
ENERGY_INDICATORS:
  IN_DATA_VERSION: "1.1"
WILDFIRES_FWI:
  IN_DATA_VERSION: "dev"
OBSALL:
  IN_DATA_VERSION: "1.0"
DATA:
  IN_DATA_VERSION: "dev"
AQUA:
  AWS_PROFILE: "development"
