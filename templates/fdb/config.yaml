type: local
engine: toc
schema: "%REQUEST.FDB_HOME%/etc/fdb/schema"
spaces:
  - handler: Default
    roots:
      - path: "%REQUEST.FDB_HOME%"
        wipe: true
        archive: true
        list: true
        retrieve: true
