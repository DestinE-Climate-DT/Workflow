CONFIGURATION:
  IFS:
    GTYPE: tco
    RESOL: 79
    LEVELS: 137
    EXPVER: hvi1
    LABEL: LWDA

JOBS: #this is NOT platform-independent and we need to discuss about it
  SIM:
    PARTITION: "compute"
    NODES: 1
    TASKS: 64
    CUSTOM_DIRECTIVES: ["#SBATCH --export=ALL", "#SBATCH --exclusive", "#SBATCH -n 64"]
